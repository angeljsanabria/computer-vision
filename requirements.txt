# Object detection
###
#Activar el entorno virtual
# con python 3.10
# curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
# python get-pip.py

# ir a la carpeta del -r
# pip install --upgrade pip
# pip install -r requirements.txt


# python3.10 -m venv .venvOK
# source .venvOK/bin/activate

###
cvzone==1.5.6
#torch==2.7.1
ultralytics
hydra-core>=1.2.0
matplotlib>=3.2.2
Pillow>=7.1.2   # 9.2.0
PyYAML>=5.3.1
requests>=2.23.0
scipy>=1.4.1
torch>=1.7.0
torchvision>=0.8.1
tqdm>=4.64.0
filterpy==1.4.5
scikit-image==0.19.3
numpy>=1.18.5 # 1.26.4
# Pin fijo (no >=): en Linux ARM algunos wheels recientes de opencv-python traen Qt
# embebida y pueden fallar al importar (undefined symbol Qt_5 / QtOpenCVPython).
# En RK3568: pip uninstall -y opencv-python opencv-contrib-python opencv-python-headless
# luego: pip install --no-cache-dir "opencv-python==4.8.1.78"
# Si esta version aun falla, probar en orden: 4.7.1.78, 4.6.0.66 (>=4.6 suele ir con ultralytics).
opencv-python==4.8.1.78
opencv-python-headless==4.8.1.78
lap>=0.4.0
streamlit==1.32.2
mediapipe==0.10.9
